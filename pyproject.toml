[project]
name = "probo_playground"
version = "0.1.0"
dependencies = [
    "matplotlib>=3.10.8",
    "numpy>=2.4.2",
    "pandas>=3.0.1",
    "pytest>=9.0.2",
    "pyyaml>=6.0.3",
]
requires-python = ">=3.11"
[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["test"]
